{
  "name": "mm5addon-parentalrating",
  "version": "0.3.21",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "pack-mmip ./src/ itunesadvisory -a -b -y",
    "start": "npm run build -- -o"
  },
  "author": "Patrick Delancy",
  "license": "MIT",
  "devDependencies": {
    "pack-mmip": "^1.2.1"
  }
}
